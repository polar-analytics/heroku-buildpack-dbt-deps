# heroku-buildpack-dbt-deps

Install dbt dependencies for dbt project in directory `BUILDPACK_DBT_DEPS_PROJECT_DIR`.

## Usage

Set environment variables `BUILDPACK_DBT_DEPS_PROJECT_DIR`.

## License

MIT
